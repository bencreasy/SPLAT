environment: "staging"
log_level: "INFO"

ground_control:
  environment:
    DEVELOPMENT_MODE: "false"
    DEBUG: "false"

monitoring:
  prometheus:
    retention_days: 15
  grafana:
    allow_anonymous: false

deployment:
  backup:
    enabled: true
    retention_days: 30
